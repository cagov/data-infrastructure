{
    "name": "Jupyter",
    "build": {
        "dockerfile": "./Dockerfile",
        "context": "."
    },
    "features": {},
    "postCreateCommand": "conda env update -n base -f images/environment.yml"
}
